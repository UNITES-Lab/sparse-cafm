global:
  device: 0
  mode: train  # ['train', 'eval']
  formulation: p(y|y_sparse)

model:
  name: ours
  pretrained: True
  weights:
  config: model-configs/swinir.yaml

surrogate:
  name: older_surrogate_mh
  weights: /playpen/mufan/levi/tianlong-chen-lab/material-super-resolution/__exps__/y-task-formulations/p(y | y_sparse)/e. surrogate standalone train-runs/2025-02-12_14-26-54_older_surrogate_mh-VGG-l[-2]+layernorm-adamW-lr=1e-4-augs=False/older_surrogate_mh-VGG-l[-2]+layernorm-adamW-lr=1e-4-augs=False_best_older_surrogate.pth

training:
  batch_size: 1
  steps_per_epoch: 1024
  epochs: 15
  loss: L1
  lr: 1e-4
  optimizer: Adam

validation:
  batch_size: 1
  steps_per_epoch: 256
  loss: L1

dataset:
  name: MOS2SEFDataset
  image_size: 512
  crop_size: 128
  num_workers: 0
  masking_ratio: 1

logging:
  root: "/playpen/mufan/levi/tianlong-chen-lab/material-super-resolution/__exps__/y-task-formulations/p(y | X_sparse, y_sparse)"
  exp_name: swinir-baseline
  result_columns: ["global_train_step", "global_val_step", "epoch", "train_loss", "val_loss", "z"]
  save_weights: true
  save_only_best_weights: true
  enable_tensorboard: false
  log_figures: false
  log_interval: 1