# **TODO**

0. Train a foundation current-map and surface-morphology model
    - 0a. combine datasets into a unified dataloader
    - 0b. generate a large-scale synthetic dataset to pre-train foundation models on w/ ControlNet
    - 0c. train models for both modes : [surface-morphology, surface current]
1. Report expert-eval %diffs for all conditions
    - 1a. datasets: [mos2-sef, sapphire, silicon]
    - 1b. modes   : [surface-morphology, surface current]
    - Note: we are only interested in *surface roughness* for morphology experiments
2. Experiments
    - 2a. seen vs. unseen datasets (equiv: synth downsampling vs real downsampling)
    - 2b. 
3. Train model with/without surrogate

# **Thoughts**

- What is the best way to do evaluation?